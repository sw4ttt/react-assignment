Created by: Oscar Marquez

## Install dependencies

In the project directory, you can run:

`npm install`

`cd server && npm install`

## Available Scripts

### `npm start`

## Testing:

Go to:

`http://localhost:3000`
